<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_user_type"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.supersupply.supersupply.UserType">

    <Button
        android:text="User"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/user_button"
        android:layout_marginBottom="90dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />

    <TextView
        android:text="Welcome to Super Supply"
        android:layout_width="220dp"
        android:layout_height="90dp"
        android:layout_marginBottom="27dp"
        android:id="@+id/textView"
        android:layout_above="@+id/vendor_button"
        android:layout_centerHorizontal="true"
        android:textAlignment="center"
        android:textSize="36sp" />

    <Button
        android:text="Vendor"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/vendor_button"
        android:layout_marginBottom="29dp"
        android:layout_above="@+id/user_button"
        android:layout_alignStart="@+id/user_button" />
</RelativeLayout>
